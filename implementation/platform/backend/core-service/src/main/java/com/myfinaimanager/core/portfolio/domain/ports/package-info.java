/**
 * Outbound ports — interfaces the business layer uses to reach external capabilities (persistence, default-investor lookup). No framework types in signatures.
 */
package com.myfinaimanager.core.portfolio.domain.ports;
