package com.myfinaimanager.core.portfolio.domain.model;

/** Lifecycle status of a portfolio. FD001 only ever creates {@code ACTIVE} portfolios (spec A12). */
public enum PortfolioStatus {
    ACTIVE
}
