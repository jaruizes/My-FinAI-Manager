/**
 * Outbound ports for external market data: {@code MarketDataPort} (latest price),
 * {@code InstrumentProfilePort} (company profile / sector), {@code FxRatePort} (currency rate).
 * Three independently replaceable interfaces; provider-neutral domain types only (AR-055).
 */
package com.myfinaimanager.core.marketdata.domain.ports;
