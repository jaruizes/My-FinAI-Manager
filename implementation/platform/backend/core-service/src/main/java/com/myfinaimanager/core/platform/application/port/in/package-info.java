/**
 * Inbound ports (convention placeholder — no classes in EN001).
 *
 * <p>Interfaces describing the use cases a capability exposes to inbound adapters. Implemented by
 * the {@code application} layer, called by {@code adapter.in.*}. Populated by FD001 onward.
 */
package com.myfinaimanager.core.platform.application.port.in;
