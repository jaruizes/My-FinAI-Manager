/**
 * Relational persistence adapter (Spring Data JPA). Implements domain.ports; maps domain <-> JPA entities. Flyway owns the schema.
 */
package com.myfinaimanager.core.portfolio.infrastructure.persistence;
