/**
 * FD005 infrastructure: persistence (Spring Data JPA over the {@code portfolio_analysis*} tables,
 * V5), the two confined cross-module adapters ({@code portfolio} and {@code ai}, AR-062 style —
 * each the sole importer of its module), the REST controller, and the async executor
 * configuration.
 */
package com.myfinaimanager.core.portfolioanalysis.infrastructure;
