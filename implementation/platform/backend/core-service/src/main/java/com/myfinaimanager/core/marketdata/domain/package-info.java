/**
 * Provider-neutral market-data domain: read models ({@code model}), outbound ports ({@code ports}),
 * and failure types ({@code exceptions}). Framework-free — no Spring, HTTP, JSON, or Finnhub type
 * (AR-002, AR-055; ArchUnit-enforced).
 */
package com.myfinaimanager.core.marketdata.domain;
