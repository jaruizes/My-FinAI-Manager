/**
 * Domain model — the Portfolio aggregate, Position, value objects, enums and deterministic domain behaviour. Framework-free (no Spring, JPA, HTTP, JDBC, messaging, serialization).
 */
package com.myfinaimanager.core.portfolio.domain.model;
