/**
 * Business layer — the create-Portfolio use case. Depends only on domain.model and domain.ports; must not depend on infrastructure. Spring annotations (DI / transactions) are allowed here (ADR-003).
 */
package com.myfinaimanager.core.portfolio.business;
