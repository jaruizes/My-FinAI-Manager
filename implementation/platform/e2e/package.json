{
  "name": "my-finai-manager-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Containerized browser E2E tests (Playwright) for the My-FinAI-Manager platform — EN002.",
  "scripts": {
    "test": "playwright test",
    "test:one": "playwright test -g"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
